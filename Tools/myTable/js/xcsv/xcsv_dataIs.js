class clsDataIs {
    constructor(parentOne) {
        this.pOne = parentOne
    }

    Empty() {
        if(this.pOne.headers == null && this.pOne.data == nulL) {
            return true}
        return false
    }
}